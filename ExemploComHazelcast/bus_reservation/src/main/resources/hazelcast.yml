hazelcast:
  cluster-name: dev
  jet:
    enabled: true
    resource-upload-enabled: true
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
      enabled: true
      member-list: hazelcast
    rest-api:
      enabled: true
      endpoint-groups:
        CLUSTER_READ: 
          enabled: true
        HEALTH_CHECK:
          enabled: true
  map:
    teste-cache:
      time-to-live-seconds: 300
      eviction: 
        size: 1000
        max-size-policy: PER_NODE
        eviction-policy: LRU